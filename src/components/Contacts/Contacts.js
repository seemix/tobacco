import React from 'react';

const Contacts = () => {
    return (
        <div className={'content_container'} style={{marginTop: '80px'}}>
            <h2>Contacts</h2>
        </div>
    );
};

export default Contacts;