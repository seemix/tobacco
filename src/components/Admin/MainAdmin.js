import React from 'react';
import Loader from '../Loader/Loader';

const MainAdmin = () => {
    return (
        <div>
            <h2>MAIN admin</h2>
            <Loader/>
        </div>
    );
};

export default MainAdmin;